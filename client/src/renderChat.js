import configFile from './config.json';
import trimMsg from './trimMsg';

export default async function renderChat(index, toUse, chatType, setChat) { const rawMsg = JSON.parse(sessionStorage.getItem('rawMsg'));
}